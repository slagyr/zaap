{:number "008"
 :status :active
 :stories [{:id "zaap-8kq" :title "Fix voice transcript accumulation — restart recognition task after each utterance"}
           {:id "zaap-a76" :title "Fix session picker: always show picker, not static text when sessions empty"}]
 :guardrails ["TDD enforced — read ~/.openclaw/skills/tdd/SKILL.md before any implementation"
              "No force unwraps (!) anywhere in codebase"
              "All new Swift files must be added to project.pbxproj"
              "Tests must pass before commit — run xcodebuild test first"
              "No third-party dependencies — Apple frameworks only"]
 :notes ["Theme: Voice Polish — fix transcript accumulation and session picker UX"
         "zaap-8kq is P0 — must land first, zaap-a76 can follow"]}
