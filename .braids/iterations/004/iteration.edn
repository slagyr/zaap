{:number "004", :status :complete, :stories [{:id "zaap-fkp", :title "Per-service path routing — base URL is /hooks, each service appends its own path"} {:id "zaap-r25", :title "Webhook test button — verify config with a ping (depends on zaap-fkp)"} {:id "zaap-4jt", :title "Send Now buttons — manual trigger for each data source (depends on zaap-fkp)"}], :guardrails ["Follow TDD — read ~/.openclaw/skills/tdd/SKILL.md before writing any code" "All new Swift files must be added to project.pbxproj" "Fix crash on invalid/missing webhook URL — graceful error handling everywhere" "Verify build compiles AND tests pass before pushing" "No build artifacts in commits" "Bump build number after all stories complete for TestFlight upload"], :notes ["App currently crashes on bad webhook config — this iteration fixes that" "zaap-fkp goes first: changes URL architecture that the other two depend on" "After all stories complete, archive and upload build 3 to TestFlight"]}